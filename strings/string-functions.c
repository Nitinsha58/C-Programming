#include<stdio.h>
#include<string.h>

int main(){
    // char name[30];
    // printf("Enter name: ");
    // gets(name); // Function to read string from user.
    
    // printf("Name: ");
    // puts(name); // Function to display string.


    // Function         Work of Function

    // // strlen()         computes string's length
    // char a[20] = "Program";
    // char b[20] = {'P', 'r', 'o', 'g', 'r','a','m','\0'};

    // // using the %zu format specifier to pritn size_t
    // printf("Length of string a  = %zu \n", strlen(a));
    // printf("Length of string b  = %zu \n", strlen(b));


    // // strcpy()         copies a strig to another
    // char str1[20] = "C Programming";
    // char str2[20];

    // // copying str1 to str2
    // strcpy(str2, str1);

    // printf(str2); // C programming


    // // strcat()         concatenates(joins) two strings
    // char str1[100] = "This is ", str2[] = "nisisk.com";

    // // concatenates str1 and str2
    // // the result string is stored in str1.
    // strcat(str1, str2);

    // puts(str1);
    // puts(str2);


    // // strcmp()         compares two strings
    // char str1[] = "abcd", str2[] = "abCd", str3[] = "abcd";
    // int result;

    // // Comparing string str1 and str2
    // result = strcmp(str1, str2);
    // printf("strcmp(str1, str2) = %d\n", result);
    
    // // comparing strings str1 and str3
    // result = strcmp(str1, str3);
    // printf("strcmp(str1, str3) = %d\n", result);
}